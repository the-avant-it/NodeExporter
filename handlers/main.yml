---
- become: yes
  block: 
  
  - name: Reload daemon
    ansible.builtin.systemd:
      daemon_reload: yes

  - name: Restart node_exporter
    ansible.builtin.systemd:
      name: node_exporter.service
      state: restarted